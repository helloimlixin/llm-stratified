# TinyViT model configuration
name: tinyvit

# Architecture
patch_size: 4
embed_dim: 192
depth: 8
num_heads: 3
mlp_ratio: 2.0
dropout: 0.1

# Timm settings (ignored for native TinyViT)
timm_model: null
timm_pretrained: true

# Compilation
compile: false
