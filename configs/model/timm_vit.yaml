# Timm ViT backbone configuration
name: timm_vit

# Use timm's pretrained ViT
timm_model: vit_tiny_patch16_224
timm_pretrained: true

# These are ignored when using timm
patch_size: 16  # Determined by timm model
embed_dim: 192
depth: 12
num_heads: 3
mlp_ratio: 4.0
dropout: 0.0

compile: false
