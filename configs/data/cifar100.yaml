# CIFAR-100 dataset configuration
name: CIFAR100
root: ${oc.env:LLM_STRATIFIED_DATA_ROOT,../data}
img_size: 32
batch_size: 128
batch_size_test: 256
num_workers: 4
subset_train: null
subset_test: null
